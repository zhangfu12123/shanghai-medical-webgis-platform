"use strict";Object.defineProperty(exports, "__esModule", {value: true});// index.ts
var _helpers = require('@turf/helpers');
var _invariant = require('@turf/invariant');
var _arc = require('arc');
function greatCircle(start, end, options = {}) {
  if (typeof options !== "object") throw new Error("options is invalid");
  const { properties = {}, npoints = 100, offset = 10 } = options;
  const startCoord = _invariant.getCoord.call(void 0, start);
  const endCoord = _invariant.getCoord.call(void 0, end);
  if (startCoord[0] === endCoord[0] && startCoord[1] === endCoord[1]) {
    const arr = Array(npoints).fill([startCoord[0], startCoord[1]]);
    return _helpers.lineString.call(void 0, arr, properties);
  }
  const generator = new (0, _arc.GreatCircle)(
    { x: startCoord[0], y: startCoord[1] },
    { x: endCoord[0], y: endCoord[1] },
    properties || {}
  );
  const line = generator.Arc(npoints, { offset });
  return line.json();
}
var index_default = greatCircle;



exports.default = index_default; exports.greatCircle = greatCircle;
//# sourceMappingURL=index.cjs.map